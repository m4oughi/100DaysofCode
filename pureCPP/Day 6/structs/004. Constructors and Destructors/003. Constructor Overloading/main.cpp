#include <iostream>

struct Circle {
    double radius;

    // Default constructor
    Circle() : radius(1.0) {}

    // Parameterized constructor
    Circle(double r) : radius(r) {}
};


int main() {


    return 0;
}