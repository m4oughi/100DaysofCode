#include <iostream>

struct Color {
    int red;
    int green;
    int blue;

    // Member initializer list for multiple members
    Color(int r, int g, int b) : red(r), green(g), blue(b) {}
};

int main() {


    return 0;
}