#include <iostream>

struct ImmutablePair {
    const int first;
    const int second;

    // Member initializer list for immutable data
    ImmutablePair(int f, int s) : first(f), second(s) {}
};

int main() {


    return 0;
}