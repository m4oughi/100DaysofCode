#include <iostream>

struct Rectangle {
    int width;
    int height;

    // Member initializer list with parameters
    Rectangle(int w, int h) : width(w), height(h) {}
};

int main() {


    return 0;
}