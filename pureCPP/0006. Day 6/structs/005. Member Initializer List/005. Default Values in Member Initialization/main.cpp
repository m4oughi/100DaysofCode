#include <iostream>

struct Box {
    int length;
    int width;
    int height;

    // Member initializer list with some default values
    Box(int l, int w = 10, int h = 5) : length(l), width(w), height(h) {}
};

int main() {


    return 0;
}