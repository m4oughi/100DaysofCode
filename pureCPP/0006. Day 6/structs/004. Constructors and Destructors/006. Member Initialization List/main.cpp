#include <iostream>

struct Book {
    std::string title;
    int pages;

    // Constructor with member initialization list
    Book(const std::string& t, int p) : title(t), pages(p) {}
};

int main() {


    return 0;
}