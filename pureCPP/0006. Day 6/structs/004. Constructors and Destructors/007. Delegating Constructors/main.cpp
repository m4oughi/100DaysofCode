#include <iostream>

struct Vector {
    double x;
    double y;

    // Default constructor
    Vector() : Vector(0.0, 0.0) {}

    // Parameterized constructor
    Vector(double xVal, double yVal) : x(xVal), y(yVal) {}
};


int main() {


    return 0;
}