#include <iostream>
#include <array>
#include <utility> // For std::move

int main() {
    std::array<std::array<int, 3>, 2> matrix = {{{1, 2, 3}, {4, 5, 6}}}; // 2x3 matrix initialization

    return 0;
}