#include <iostream>
#include <deque>

int main() {
    std::deque<int> myDeque = {10, 20, 30, 40};

    // Getting the size of the deque
    std::cout << "Size of deque: " << myDeque.size() << std::endl;  // Output: 4

    return 0;
}
