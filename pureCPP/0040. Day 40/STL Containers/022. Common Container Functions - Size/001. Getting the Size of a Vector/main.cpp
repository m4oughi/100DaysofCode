#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec = {10, 20, 30, 40, 50};

    // Getting the size of the vector
    std::cout << "Size of vector: " << vec.size() << std::endl;  // Output: 5

    return 0;
}
