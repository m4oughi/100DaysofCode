#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec;

    std::cout << "Maximum size of vector: " << vec.max_size() << "\n";
    // Prints the maximum number of elements the system can handle

    return 0;
}
